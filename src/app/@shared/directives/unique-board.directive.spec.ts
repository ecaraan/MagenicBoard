import { UniqueBoardDirective } from './unique-board.directive';

describe('UniqueBoardDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueBoardDirective();
    expect(directive).toBeTruthy();
  });
});
