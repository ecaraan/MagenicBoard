import { UniqueCardDirective } from './unique-card.directive';

describe('UniqueCardDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueCardDirective();
    expect(directive).toBeTruthy();
  });
});
